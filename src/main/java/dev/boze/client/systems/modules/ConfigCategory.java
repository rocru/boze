package dev.boze.client.systems.modules;

public enum ConfigCategory {
    Main,
    Visuals,
    Client;

    // $VF: synthetic method
    private static ConfigCategory[] method778() {
        return new ConfigCategory[]{Main, Visuals, Client};
    }
}
