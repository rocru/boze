package dev.boze.client.systems.accounts;

public enum AccountType {
    Microsoft,
    Altening,
    Cracked;

    // $VF: synthetic method
    private static AccountType[] method881() {
        return new AccountType[]{Microsoft, Altening, Cracked};
    }
}
